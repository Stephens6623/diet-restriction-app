{
  "name": "diet-restriction-app",
  "version": "1.0.0",
  "description": "an app for dietary restrictions and people needing specific knowledge of certain grocery items",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "server": "node server/index.js",
    "client": "cd client && npm start",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "cd client && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "diet",
    "restrictions",
    "allergens",
    "food",
    "scanner"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^4.21.2",
    "multer": "^2.0.2",
    "openai": "^6.8.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
